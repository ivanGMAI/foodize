# Shell scripts must use LF line endings — they are executed by bash and break
# with Windows CRLF (e.g. `set -o pipefail\r` → "invalid option name").
*.sh text eol=lf
tools/tg.sh text eol=lf
tools/backup.sh text eol=lf
tools/restore.sh text eol=lf
